{
  "name": "kommo-private-chatbot-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend webhook for Kommo private chatbot widget",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/kommo.js && node --check src/response-builder.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  }
}
